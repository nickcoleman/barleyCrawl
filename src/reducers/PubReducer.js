import React from 'react'

import {
  INPUT_UPDATE,
} from '../actions'

const INITIAL_STATE = {
  location: '',
  name: ''
}

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {

    default:
      return state
  }
}
